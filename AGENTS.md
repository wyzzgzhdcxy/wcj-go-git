# 强制编码规则
你拥有文件读取工具。
⚠️每一次生成任何业务代码之前，**必须先执行动作：读取 D:\code\github\wcj-go-common 目录下全部*.go源码文件**。
完整浏览common包所有工具函数、结构体、实现逻辑。

硬性约束：
1. 如果common包里面已经存在对应工具能力，**严禁手写实现、禁止重复造轮子，不要复制common源码到业务代码**。
2. 直接 import "你的模块路径/common"，调用里面现成函数。
3. 只有common完全不存在的逻辑，才编写新代码。
4. 禁止忽略common目录，不许跳过读取common的步骤。
5. 生成代码前必须看过common源码，不允许只靠猜测。

输出代码前自我检查：我有没有复用common已有能力？如果有手写工具函数，直接删除替换为common调用。
